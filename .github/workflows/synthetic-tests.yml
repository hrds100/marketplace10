name: Synthetic Health Checks — hub.nfstay.com

on:
  schedule:
    - cron: '*/5 * * * *' # Every 5 minutes
  workflow_dispatch: # Allow manual trigger

jobs:
  synthetic-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: marketplace10/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: marketplace10

      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps
        working-directory: marketplace10

      - name: Run synthetic health checks
        run: npx playwright test e2e/synthetic/ --reporter=list
        working-directory: marketplace10
        env:
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: synthetic-results-marketplace-${{ github.run_id }}
          path: marketplace10/test-results/
          retention-days: 7
